import React from "react";

import MenuNav from "../menus/index";

import "./index.less";

const SliderNav = (props: any) => {
  return (
    <div className="crawler-slider-nav">
      <MenuNav />
    </div>
  );
};

export default SliderNav;
